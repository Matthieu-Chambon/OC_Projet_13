from django.apps import AppConfig


class ProfilesConfig(AppConfig):
    """
    Configuration de l'application 'profiles'

    Args:
        AppConfig (class): Classe de base pour la configuration des applications Django
    """
    name = 'profiles'
